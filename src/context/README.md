# Context

Owns structured context assembly:

- required session and task context
- memory and artifact retrieval
- plugin-provided references
- ranking, trimming, and summarization
- provenance metadata

Context output should remain inspectable before it is passed to an agent run.

